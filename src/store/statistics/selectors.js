export const selectDate = state => state.statistics.selectedDate;

export const selectSummary = state => state.statistics.summary;

export const selectIsLoading = state => state.statistics.isLoading;
